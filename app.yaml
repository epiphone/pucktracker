application: pucktracker
version: 1
runtime: python27
api_version: 1
threadsafe: true

handlers:
- url: /.*
  script: main.gae_app

libraries:
- name: lxml
  version: latest

builtins:
- deferred: on
- remote_api: on